# Orchestration & Database
supabase
pydantic
python-dotenv==1.0.0

# LLM / AI
anthropic

# Geometry & CAD Processing
ezdxf==1.1.4

# 3D Processing
# trimesh 4.0.8's polygon-extrusion code breaks under numpy>=2 (2D cross product removed);
# 4.12.2 is the minimum verified working version.
trimesh==4.12.2
numpy
shapely==2.1.2
# extrude_polygon()'s triangulation backend
mapbox_earcut==2.0.0
# ray-mesh intersection's bounds tree (agent_b/3d_room_builder.py control-map rendering)
rtree==1.4.1
Pillow

# Optional: web framework if we want to run orchestrator as a server receiving webhooks from n8n
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
anyio<4.0.0
PyPDF2
requests
