# Orchestration & Database
supabase
pydantic
python-dotenv==1.0.0

# LLM / AI
anthropic
# Video-native perceptual QA (agent_b/qa/gemini_judge.py). Gemini is used rather than
# Claude here specifically because it accepts video as a native input -- the QA failure
# modes that matter (flicker, walls breathing, incoherent parallax) only exist in motion
# and are invisible to still-frame sampling.
google-genai

# Geometry & CAD Processing
ezdxf==1.1.4

# 3D Processing
# trimesh 4.0.8's polygon-extrusion code breaks under numpy>=2 (2D cross product removed);
# 4.12.2 is the minimum verified working version.
trimesh==4.12.2
numpy
shapely==2.1.2
# extrude_polygon()'s triangulation backend
mapbox_earcut==2.0.0
# ray-mesh intersection's bounds tree (agent_b/3d_room_builder.py control-map rendering)
rtree==1.4.1
# Embree BVH backend for ray casting. Not strictly required -- trimesh falls back to its
# pure-Python intersector -- but the gap is 676x on the four-room mesh (0.17s vs 115s per
# frame), which is the difference between an 8-second render and a 94-minute one.
embreex
Pillow
# rank correlation + mask dilation for numeric geometry QA (agent_b/qa/depth_metrics.py);
# also required by trimesh.fix_normals()
scipy
# top-down camera-route preview only (agent_b/plan_preview.py), imported lazily so the
# render path and CI geometry job do not depend on it
matplotlib

# Optional: web framework if we want to run orchestrator as a server receiving webhooks from n8n
fastapi==0.104.1
uvicorn==0.24.0
python-multipart
anyio<4.0.0
PyPDF2
requests
# PDF floor-plan vector extraction (agent_b/pdf_vector.py). Needed specifically for its
# marked-content layer attribution: a regex reader over the decompressed streams agrees on
# total geometry to 0.1% but mis-assigns 11% of wall segments, because BDC/EMC nest.
PyMuPDF
# watershed segmentation, used to divide a wall-enclosed region that contains several room
# labels (agent_b/wall_regions.py). Only needed for the PDF path.
scikit-image
